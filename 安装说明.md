# 短视频下载器 v4.1 安装指南

## 版本更新 (v4.1)
- 🌐 **新增 HuggingFace 国内镜像** - hf-mirror.com，解决模型下载超时问题
- 🎵 **链接ASR优化** - 只下载音频流，不再下载完整视频
- 🧹 **临时文件自动清理** - 识别完成后自动删除临时音频文件

---

## 版本更新 (v4.0)
- ✨ **新增功能：音频转文字 (ASR)** - 使用 faster-whisper 语音识别
- 🎤 支持将视频/音频中的语音识别为文字
- 📝 输出文字稿(.txt)和字幕(.srt)两种格式
- 🤖 支持选择模型大小：tiny/base/small/medium

---

## 安装步骤

### 1. 安装依赖

使用 pip 安装所有依赖：

```bash
pip install -r requirements.txt
```

或者手动安装：

```bash
pip install yt-dlp requests pyperclip faster-whisper
```

### 2. 安装 ffmpeg（必须）

ffmpeg 用于音视频处理，必需安装：

**Windows 方式一：winget（推荐）**
```bash
winget install ffmpeg
```

**Windows 方式二：Chocolatey**
```bash
choco install ffmpeg
```

**Windows 方式三：手动下载**
1. 访问 https://ffmpeg.org/download.html
2. 下载 Windows 构建版本
3. 解压到任意目录（如 C:\ffmpeg）
4. 将 bin 目录添加到系统 PATH

**验证安装：**
```bash
ffmpeg -version
```

### 3. 安装 faster-whisper（ASR功能必需）

**首次运行时会自动下载模型（约 500MB-3GB）**

模型选择建议：
| 模型 | 大小 | 显存需求 | 推荐度 | 说明 |
|------|------|----------|--------|------|
| tiny | 39M | ~1GB | ⭐ | 速度快，准确率一般 |
| base | 74M | ~1GB | ⭐⭐ | 快速，基础识别 |
| small | 244M | ~2GB | ⭐⭐⭐ | **推荐首选**，中文效果好 |
| medium | 769M | ~5GB | ⭐⭐⭐ | 高精度，RTX 5060 Ti 可用 |

---

## 卸载重装（如需）

如果之前安装了旧版本，升级安装：

```bash
pip install --upgrade yt-dlp requests pyperclip
pip install --upgrade faster-whisper
```

---

## 功能说明

### 标签页1：下载模式
- 复制视频链接后自动下载视频+音频
- 支持平台：抖音、B站、小红书、快手
- 视频保存在 `downloads/视频/` 目录
- 音频保存在 `downloads/音频/` 目录

### 标签页2：音频转文字 (ASR)
- **方式一**：选择本地音频/视频文件进行转文字
- **方式二**：输入视频链接，自动下载并转文字
- 支持格式：MP3、WAV、M4A、MP4、AVI、MKV 等
- 输出结果：
  - 文字稿 (`.txt`) - 完整文本
  - 字幕文件 (`.srt`) - 带时间戳的字幕

---

## 常见问题

### Q: 安装 faster-whisper 报错？
**A:** 确保已安装 Visual Studio Build Tools (C++ 编译环境)

### Q: CUDA 相关错误？
**A:** 确保 NVIDIA 驱动已安装，并支持 CUDA 12.x

### Q: 模型下载失败？
**A:** 首次运行会自动下载模型，可能需要代理访问 Hugging Face
  ```bash
  # 设置代理（如需要）
  set HTTP_PROXY=http://127.0.0.1:7890
  set HTTPS_PROXY=http://127.0.0.1:7890
  ```

### Q: 显存不足？
**A:** 
- 选择更小的模型（如 small 而非 medium）
- 关闭其他占用 GPU 的程序
- Windows 可尝试增加虚拟内存

### Q: 识别结果不准确？
**A:**
- 确保音频清晰、无过多背景噪音
- 选择合适的识别语言
- medium 模型比 small 中文�