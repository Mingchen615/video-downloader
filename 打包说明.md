# 短视频无水印下载器

## 📌 功能特点

- **图形界面**：简洁易用的 Tkinter GUI
- **多平台支持**：抖音、B站、小红书、快手
- **无水印下载**：自动提取无水印视频
- **信息展示**：显示视频标题、作者、描述等信息
- **Windows exe**：双击即用，无需安装 Python 环境

---

## 🚀 快速使用

### 方法一：直接运行 Python 脚本

```bash
# 1. 安装依赖
pip install yt-dlp

# 2. 运行脚本
python video_downloader.py
```

### 方法二：使用打包好的 exe 文件

1. 双击 `video_downloader.exe`
2. 粘贴视频链接
3. 点击下载

---

## 📦 打包说明

### 自动打包（推荐）

在您的 Windows 电脑上，双击运行 `一键打包.bat`，会自动完成所有打包工作。

### 手动打包

如果自动打包失败，请按以下步骤操作：

```bash
# 1. 安装打包工具
pip install pyinstaller

# 2. 执行打包
pyinstaller --onefile --windowed --icon=icon.ico video_downloader.py

# 3. 找到生成的 exe 文件
# 位于 dist/video_downloader.exe
```

### PyInstaller 详细参数说明

| 参数 | 作用 |
|------|------|
| `--onefile` | 打包成单个 exe 文件 |
| `--windowed` | 不显示命令行窗口（GUI专用） |
| `--icon=xxx.ico` | 设置程序图标 |
| `--name=xxx` | 指定输出文件名 |
| `--add-data` | 附加额外文件 |

### 完整打包命令示例

```bash
pyinstaller --onefile --windowed --name "短视频下载器" video_downloader.py
```

---

## 🔧 依赖说明

| 依赖包 | 版本 | 用途 |
|--------|------|------|
| yt-dlp | 最新 | 视频下载核心 |
| tkinter | 内置 | 图形界面（Python自带） |

---

## 📁 文件结构

```
短视频下载器/
├── video_downloader.py    # 主程序脚本
├── 一键打包.bat           # 自动打包脚本
├── 打包说明.md            # 本文件
├── downloads/             # 下载的视频存放目录
│   └── (视频文件)
└── dist/                  # 打包输出目录（打包后生成）
    └── video_downloader.exe
```

---

## ⚠️ 注意事项

1. **抖音链接**：支持长链接和短链接（v.douyin.com）
2. **B站链接**：支持 BV 号链接和 b23.tv 短链接
3. **小红书**：支持 xhslink.com 短链接
4. **快手**：支持 kuaishou.com 和 v.kuaishou.com

### 常见问题

**Q: 下载失败怎么办？**
- 确保 yt-dlp 是最新版本：`pip install --upgrade yt-dlp`
- 检查网络连接
- 某些视频可能需要登录才能下载

**Q: 文件名乱码？**
- 程序会自动清理文件名中的非法字符
- 如果仍有乱码，可能是视频平台返回的标题编码问题

**Q: 进度条不动？**
- 视频正在下载中，请耐心等待
- 大视频可能需要较长时间

---

## 🛠️ 技术实现

- **GUI框架**：Tkinter（Python内置）
- **下载引擎**：yt-dlp
- **打包工具**：PyInstaller
- **支持平台**：Windows 7/8/10/11

---

## 📝 许可证

MIT License - 可自由使用、修改和分发
