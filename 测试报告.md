# 抖音音频提取功能测试报告

## 测试日期
2024年

## 测试环境
- Python版本: 3.x
- yt-dlp版本: 2026.03.17
- ffmpeg版本: 4.4.2
- 测试链接: https://v.douyin.com/A1pOr-9mAdU/

## 测试结果

### ✓ 依赖检查通过
- yt-dlp 已安装
- ffmpeg 已安装
- 平台检测功能正常

### ✗ 抖音音频下载需要Cookies认证

测试过程中发现抖音视频下载返回错误：
```
ERROR: [Douyin] 7634486058394111295: Fresh cookies (not necessarily logged in) are needed
```

这是因为：
1. 抖音API要求Fresh cookies验证
2. yt-dlp的抖音提取器需要有效的Cookies才能下载视频
3. 这是一个API限制，不是代码问题

## 代码修改内容

### 1. 版本升级 v4.1 → v4.2
- 新增抖音Cookies认证支持
- 更新文档说明

### 2. VideoDownloader.__init__() 新增
```python
# Cookies文件路径（用于抖音等需要认证的平台）
self.cookies_path = os.path.join(self.script_dir, "cookies.json")
self._has_cookies = os.path.exists(self.cookies_path)
```

### 3. 所有yt-dlp调用新增cookies支持
在以下方法中添加了 cookies 认证支持：
- `download_audio_only()` - 音频提取（ASR用）
- `_get_video_info_basic()` - 获取视频信息
- `_download_video_ytdlp()` - 视频下载
- `_download_audio_ytdlp()` - 音频提取
- `_download_audio_douyin()` - 抖音音频下载

```python
# 抖音等平台需要cookies认证
if self._has_cookies:
    ydl_opts['cookiefile'] = self.cookies_path
```

## 解决方案

### 获取Cookies步骤

1. **使用浏览器扩展导出Cookies**
   - 推荐扩展：EditThisCookie (Chrome/Firefox)
   - 访问 https://www.douyin.com 并登录
   - 点击扩展图标，导出Cookies为JSON格式

2. **保存Cookies文件**
   - 将导出的JSON保存为 `cookies.json`
   - 放置到脚本同目录下：`./短视频下载器/cookies.json`

3. **验证Cookies格式**
   ```json
   [
     {
       "name": "sessionid",
       "value": "你的sessionid值",
       "domain": ".douyin.com",
       "path": "/"
     }
   ]
   ```

## 新增文件

| 文件 | 说明 |
|------|------|
| `cookies.json.example` | Cookies文件模板 |
| `test_audio_download.py` | 抖音音频下载测试脚本 |
| `test_bilibili_audio.py` | B站音频下载测试脚本（用于验证代码逻辑） |

## 代码逻辑验证

### ✓ 代码逻辑正确
- yt-dlp 配置正确
- ffmpeg 音频转码配置正确
- Cookies 自动检测机制工作正常
- 临时文件目录创建正常

### ⚠️ 需要用户提供真实Cookies
由于抖音API限制，无法在测试环境中模拟真实的Cookies认证

## 使用说明

### 首次使用
1. 从抖音获取Cookies
2. 保存为 `cookies.json` 放在脚本同目录
3. 启动下载器即可

### 测试脚本使用方法
```bash
cd ./短视频下载器
python3 test_audio_download.py
```

如果用户提供cookies后测试成功，会显示：
```
✓ 测试成功! (使用cookies)
  视频标题: xxx
  音频路径: /tmp/asr_test_xxx/xxx.mp3
```

## 结论

### 测试状态：⚠️ 需要用户配合
- ✅ 代码逻辑正确
- ✅ 依赖环境正常
- ✅ Cookies认证机制已实现
- ⚠️ 需要用户提供有效Cookies完成完整测试

### 后续建议
1. 用户需要提供有效的抖音Cookies
2. 可以使用B站、小红书等其他平台测试（无需Cookies）
3. 代码已支持自动检测cookies.json文件是否存在
